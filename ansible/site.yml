---

- hosts: all
  gather_facts: true
  become: yes
  become_method: sudo

  vars_files:
    - group_vars/vars.yml
    - group_vars/users.yml
  
  roles:
    - roles/stack
    - roles/nginx
    - roles/docker
    - roles/whitespace
    - roles/users